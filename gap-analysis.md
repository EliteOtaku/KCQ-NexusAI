# TV ↔ KCQ 差距比对清单（gap-analysis）

> TV 侧依据：本目录 notes.md + 截图（cn.tradingview.com 匿名中文态，2026-09-18 采集）。
> KCQ 侧依据：packages/nexus-shell 实际源码（TopBar.tsx 60 行 / SettingsDialog.tsx 121 行 / ChartContextMenu.tsx /
> DrawingToolbar.tsx 208 行 / drawingTools.ts 18 工具 / periods.ts 8 周期 / ObjectTreePanel / LegendBar /
> IndicatorPanel / SymbolPicker / WatchlistPanel / TemplatePanel / SourceManagerDialog / DrawingStyleFlybar）
> 及引擎 features（alerts / replay / chartTypes 已有未接壳）。
> 归属批次定义（backlog 拍板）：**T1** = 顶栏形态重做 + 图表属性大设置（含对数/百分比/自动缩放）+ 撤销重做（先绘图域）
> + 底部周期条 + 右缘空白扩展 + 多布局上限三分屏；**T2+** = 告警/回放/类型接线、数据窗口、绘图工具按频率补等。
> 标注：`需引擎扩展` = 壳层做不了、要动 packages/core。

## A. 顶栏形态（T1 范围域）

| TV 能力 | KCQ 现状 | 归属 |
|---|---|---|
| 品种搜索弹层（搜索+资产类别 tab+交易所徽章+延长会话） | 有：SymbolPicker（搜索/最近使用/全部品种，聚合 searchInstruments），无资产类别 tab 与交易所徽章分层 | T1（顶栏重做时顺带简化版对齐，深度列表归 T4+ 不做） |
| 周期快捷按钮（当前值+分组菜单+收藏） | 有：TopBar 原生 select + optgroup 分组（periods.ts 8 周期）；无收藏、无秒/tick/范围档 | T1（形态改为按钮+菜单；周期档位保持 KCQ 数据源能力，不追 TV 全量） |
| K线类型下拉（21 种类型菜单） | 无入口（引擎 chartTypes Renko/RangeBars/PnF 已有未接壳，常规类型引擎全有） | T2+（类型接线批次） |
| 指标入口（指标/度量/策略弹层） | 有简版：IndicatorPanel（列表添加，无搜索分类树/热度/收藏） | T4+（增强） |
| 指标模板 | 有：TemplatePanel（绘图模板）；指标模板无 | 不做（暂无需求） |
| 撤销 / 重做按钮（禁用态→激活） | 无任何 undo/redo UI；引擎无撤销栈 | **T1**（先绘图域；`需引擎扩展`：drawing 命令栈） |
| 告警按钮+面板 | 无 UI；引擎 features/alerts 已有 | T2+（告警接线） |
| 回放按钮+控制条 | 无 UI；引擎 features/replay 已有 | T2+（回放接线） |
| 布局设置（1-9 格菜单） | 无（单图表 ChartStage） | **T1**（上限三分屏：s/2h/2v/3h/3v/2-1/1-2 子集，每格独立品种；TV 其余档不追） |
| 保存布局 / 管理布局 | 无（壳偏好走 storage.ts 本地） | 不做（匿名态 KCQ 无账号体系，布局持久化本地化即可） |
| 快速搜索（全局） | 无 | 不做 |
| 设置（图表属性对话框入口） | 有：TopBar ⚙ → SettingsDialog（壳偏好版） | **T1**（顶栏重做保留入口；对话框本身见 B 域） |
| 全屏 / 快照 / 交易 / 发表 | 无 | 不做（KCQ 非社区/交易场景） |
| 切换数据类型（行情源徽章） | 有等价：SourceManagerDialog 聚合源管理（状态/开关/地址/当前源） | 不做（KCQ 数据源模型更强，保持自有形态） |
| 比较商品弹层 | 无 | T3+（比对/叠加能力，引擎 comparison 子状态已有基础） |

## B. 图表属性大设置（T1 范围域）

TV 结构：模态 + 左侧 7 tab（商品代码/状态行/坐标和线条/版面/交易/警报/事件）+ 模板/取消/确认。
KCQ 现状：SettingsDialog 仅 壳偏好（数据源/主题/磁吸/保持绘图/自动套用），无任何图表视觉属性。

| TV 设置项 | KCQ 现状 | 归属 |
|---|---|---|
| K线样式（主体/边框/影线双色、基于前收盘价变色） | 无 UI（引擎 styles 有 token 化颜色体系） | **T1**（大设置首版含基础 K 线配色） |
| 状态行（Logo/标题/图表值/变化值/成交量/买-卖按钮/指标值显隐） | 有简版：LegendBar（O/H/L/C/量 + 指标行显隐/设置/移除） | **T1**（大设置内做显隐开关，对齐 KCQ 图例粒度；Logo 等不做） |
| **坐标模式（自动/对数/百分比）** | 无（价格轴纯线性；引擎 priceScale 无 log/percent 模式） | **T1** + `需引擎扩展`（priceScale 变换模式 + 图例/坐标轴标签联动） |
| **右缘价格轴 hover 快捷钮 A / L**（hover 轴下端浮现：A=自动比例、L=对数坐标，一键切换；[9e] 截图基准；用户反馈 KCQ 对数坐标埋太深——T1 必须含此快捷入口） | 无（引擎无 log/percent 模式，壳层无入口） | **T1** + `需引擎扩展`（priceScale 模式先行，快捷钮随壳层） |
| 锁定价格对K线比例（比例值输入） | 无 | T4+（高级；随对数坐标后续） |
| 坐标放置（自动/右/左） | 无 | T4+ |
| 价格标签（无重叠标签/加号按钮/当前K线结束倒计时） | 无（倒计时 KCQ 图例亦无） | T4+（倒计时项归 T2+ 图例增强更合适，先挂 T4+） |
| 时间坐标（星期几/日期格式/24 小时制/保存左缘位置） | 无 UI（dateFormat 引擎已有能力） | T4+ |
| 版面（背景/网格线/十字线/水印/导航/窗格显隐、边距 上10% 下8% 右10%） | 有主题（light/dark/auto tokens）；网格/十字线开关无 UI | **T1**（大设置首版含背景+网格+十字线显隐；边距/水印不做） |
| 交易 / 警报 / 事件三 tab | 不适用（KCQ 无交易面板；警报 T2+ 接线时按需加 tab） | 警报=T2+；交易/事件=不做 |
| 设置模板（保存/套用图表模板） | 有：TemplatePanel（绘图模板，非图表模板） | T4+（可复用模板面板机制） |
| 精确度 / 时区 / 调整股息 | 时区=KCQ 会话时区体系已由数据层处理（marketSessions）；精确度/股息调整无 | T4+（时区切换 UI 挂 T4+，数据层已具备） |

## C. 撤销 / 重做（T1 范围域）

| TV 能力 | KCQ 现状 | 归属 |
|---|---|---|
| 顶栏撤销/重做按钮（含禁用态；覆盖绘图增删改） | 无 UI、无命令栈；绘图操作直连引擎 action（增/删/改即时生效） | **T1** + `需引擎扩展`（drawing 域命令栈：create/update/delete 可逆化；壳层只做按钮与快捷键 Ctrl+Z/Y 绑定） |
| 图元级"重置样式"（对象树/浮条内） | DrawingStyleFlybar 有模板套用，无重置 | T4+ |

## D. 底部周期条（T1 范围域）

| TV 能力 | KCQ 现状 | 归属 |
|---|---|---|
| 时间范围快捷条（1天/5天/1个月/3个月/6个月/YTD/1年/5年/全部，范围↔周期联动 tooltip） | 无（缩放仅滚轮/拖拽；引擎 viewport 有 visibleRange） | **T1** + `需引擎扩展`（范围→(周期,barCount) 解析器；或以"可见根数+自动周期降档"实现，设计文档先行） |
| 前往到（跳转日期） | 无（backlog 已登记引擎缺 scrollToDataIndex 公开 API） | T2+（随引擎大礼包 scrollToDataIndex） |
| 时区显示（03:52:11 UTC+8） | 无 UI（数据层会话时区已处理） | T4+ |
| 调整股息数据 ADJ | 无 | 不做（数据源侧能力，KCQ 自有数据链路） |

## E. 右缘空白扩展（T1 范围域，用户明确短板）

| TV 能力 | KCQ 现状 | 归属 |
|---|---|---|
| 图表右缘保留空白区（光标/新绘图可越过最新 K 线） | 无：viewport 以数据范围为界，右缘即最新 K 线 | **T1** + `需引擎扩展`（viewport 允许 trailing 空白偏移 + 十字线/绘图命中延伸到空白区） |
| 空白区时间/价格外推刻度（时间轴延续未来刻度、价格轴照常） | 无 | **T1**（时间轴外推刻度=引擎 timeAxis；与上项同批） |
| 倒数第二价格标签 / 未来时间标签样式 | 未细采（与右缘外推同域） | **T1**（实现时以截图 01 整页右缘为视觉基准） |
| **"滚动到最近的K线"悬浮按钮**（hover 价格轴浮现，»图标，Tooltip"滚动到最近的K线 Alt+Shift+→"，点击右缘对齐最新 K 线；按钮下方另有小 ∨ 疑为自动滚动选项） | 无（缩放/平移后无一键回最新） | **T1**（与右缘空白同批：viewport 跳最新+跟随；`需引擎扩展`面小——引擎 viewport 状态已有 barSpacing/rightOffset，设 rightOffset=空白色值即可） |

## F. 多布局（T1 范围域：上限三分屏）

| TV 能力 | KCQ 现状 | 归属 |
|---|---|---|
| 1 格 s | 有（当前单图表） | 现状保留 |
| 2 格 2h / 2v | 无 | **T1**（壳层多 ChartStage 实例 + 布局切分；每格独立品种/周期经现有 shell 状态扩展） |
| 3 格 3h / 3v / 2-1 / 1-2 | 无 | **T1**（上限；TV 4-9 格档不追） |
| 每格独立品种+同步切换（TV 保存布局含全部品种周期） | 无 | T1 简化：每格独立状态即可，不做 TV 式布局云保存 |

## G. 其余域（T2+ / 不做）

| TV 能力 | KCQ 现状 | 归属 |
|---|---|---|
| 对象树（树列表/建组/克隆/移动到） | 有：ObjectTreePanel（列表+显隐/锁定/删除） | T4+（建组/克隆等增强） |
| 数据窗口（十字线联动 OHLC+指标值） | 无（图例栏承担部分信息） | T2+（backlog 已列） |
| 图表空白处右键菜单 | 有：ChartContextMenu 空白分支（切换主题/周期/添加指标 3 项） | **新批次 H**（TV 基准已补采 [4a]：复制/粘贴价格格、按坐标添加警报、对象树入口、图表模板、移除全部绘图/指标、重置视图；交易三项不采） |
| 图元右键菜单 | 有图元右键：ChartContextMenu 命中分支（样式/锁定/删除 3 项，"样式"仅选中不走对话框） | **新批次 H**（TV 基准已补采 [4b]：按 kind 动态警报项/模板▸/视觉顺序▸/时间周期可见性▸/对象...▸/克隆 Ctrl+Drag/复制/锁定/隐藏/移除/设置...） |
| **模板快速调用（图元右键→模板子菜单）**：保存为.../应用默认/当前模板垃圾桶快速删除/已存模板点击即应用 | 有 per-kind 模板系统（TemplatePanel 保存/套用/重命名/删除/自动套用 + Flybar 模板按钮），但**入口不在右键菜单**：调用需先选中图元→Flybar 模板弹层；无"应用默认"；无子菜单内快速删除 | **新批次 H**（壳层：模板子菜单嵌入右键菜单 + 应用默认 + 行内删除；模板引擎现成，纯接线） |
| 视觉顺序子菜单（z-index 置顶/置底） | 引擎 DrawingObject.zIndex 字段存在，无 UI | H 后半（低优先） |
| 告警创建（条件/价格/通知） | 引擎 features/alerts 已有，无 UI | T2+ |
| 回放控制条 | 引擎 features/replay 已有，无 UI | T2+ |
| K线类型 21 种（含 Renko/PnF/Range） | 引擎 chartTypes 三异形已有未接壳；常规类型引擎全有 | T2+ |
| 绘图工具全集（TV 数十种，见 notes [2]） | 有 18 种（线/通道/斐波那契/矩形/箭头/测量/橡皮擦…），分组 lines/channels/annotations | T4+（按使用频率补：优先水平射线/垂直线已有，次补通道变体；江恩/波浪/形态远期） |
| 工具组 flyout + 收藏 + 快捷键 | 有：工具收藏（drawingTools favorite）+ 组内切换；无 Alt+ 快捷键 | T4+（快捷键另见 ShortcutsOverlay 已有壳级覆盖层） |
| 磁铁档位（off/weak/strong 三档 + tooltip 说明） | 有：引擎 setMagnetMode 三档 + 设置对话框下拉（语义 SSOT：docs/design/drawing-interaction-hardening.md） | 已对齐（不追 TV 的"磁铁"组内子档差异） |
| 继续绘图开关 | 有：stay 保持绘图模式 | 已对齐 |
| 锁定全部/隐藏全部绘图开关 | 有锁定/隐藏按图元（对象树+浮条）；无全局开关 | T4+ |
| Watchlist（分组/列排序/详情财务页） | 有：WatchlistPanel 简版 | T4+（详情页不做，KCQ 有自有品种信息路径） |
| 品种比较叠加 | 无 | T3+ |
| 指标弹层搜索/热度/收藏 | 有简版 IndicatorPanel | T4+ |
| 快照/全屏/发表/交易 | 无 | 不做 |

## H. 图元属性 / 参数 / 模板管理补课（2026-09-18 用户拍板新批次，优先级高于 T2+）

> 用户判断：KCQ 很多工具/绘图组件（如水平线）"没有属性"。核实后的精确现状与缺口如下，
> TV 基准 = [4b] 待补图（图元右键菜单 + 图元属性对话框 样式/输入/可见性三 tab）。

| TV 能力 | KCQ 现状 | 归属 |
|---|---|---|
| 图元属性对话框（右键图元→设置 或 双击；TV 实际 tab = **样式/坐标/可见范围**） | 无图元设置对话框；仅有 DrawingStyleFlybar 选中浮条 | **新批次 H**（壳层：图元设置对话框框架） |
| 样式 tab（线段配色/延伸/levels 档位表/使用一种颜色/背景/标签/字体，含 params 编辑混排——见 [4c]） | 有：Flybar 8 样式键（stroke/strokeWidth/strokeStyle/fill/fillOpacity/pointRadius/textColor/fontSize，引擎 getBatchStyleKeys 批量交集）；**延伸/档位表/标签格式 无** | 框架=H；levels 档位表=`需引擎扩展`（见下行 params）；延伸=`需引擎扩展`（渲染端） |
| **params 参数编辑**：levels 档位表在**样式 tab**（每行 = ☑显隐 + 数值可编辑输入框 + 独立颜色，"使用一种颜色"开关统一覆盖）；锚点坐标在**坐标 tab** | **无任何 UI**：引擎 DrawingObject.params 泛型存在但无 schema 无编辑入口；锚点改写仅拖拽（无数值输入） | **新批次 H** + `需引擎扩展`（每 kind 的 params schema 声明——fib levels 表 + 数值锚点改写 API——水平线价格/锚点坐标属坐标 tab 域） |
| 坐标 tab（锚点时间/价格数值编辑；水平线=价格输入） | 无（仅拖拽改锚点） | H（依赖锚点数值改写 API，与上行同项） |
| 可见范围 tab（按周期/图表类型显隐） | 有 visible 总开关（Flybar/对象树），无按周期显隐 | H 后半（低优先） |
| 属性对话框底部"模板 ∨"（对话框内模板管理，TV 模板第 4 入口） | 无（模板在 Flybar/TemplatePanel） | H（随对话框框架） |
| "基于对数坐标的Fib水平"（fib 档位与对数轴联动） | 无（依赖 T1 对数坐标先行） | H（前置=T1 priceScale 对数模式） |
| 复制/粘贴价格格（右键菜单） | 无 | H（纯壳层，读十字线价） |
| 克隆图元（TV 图元菜单"克隆"） | 无（壳有 Ctrl+拖拽复制？仅 Shift 多选/Ctrl 拖拽复制在 risks 提及壳侧保留） | H（克隆=anchor 平移复制，引擎 createDrawing 可支撑） |
| 添加到收藏（图元菜单） | 无 | 不做（低价值） |
| 图表模板（整图模板，空白右键菜单"图表模板▸" + 设置对话框底部"模板∨"双入口） | 无；有绘图模板（TemplatePanel per-kind）+ 指标面板保存 | H 后半（图表模板=设置页全部内容快照（样式+坐标+指标集），交互基准 [3h][3i]：应用默认/保存为.../日期自动命名+自定义名列表/列表项 hover 垃圾桶快速删除；复用 template 机制） |
| **模板管理统一范式**（用户拍板可照搬）：三种模板域（图表/绘图/指标）同一套交互——`应用默认 + 保存为... + 列表（点击应用 / hover 垃圾桶删除）`，仅数据域不同 | KCQ 模板能力分散在 TemplatePanel/Flybar，无统一交互范式 | **H 批实施约束**：做一套可复用模板管理组件，三处入口（右键子菜单/对话框底部下拉/面板）仅换数据域 |
| 指标模板 | 无 | H 后半（随 T2+ 指标接线评估） |

**H 批次范围建议**（待用户确认后写提示词）：①图元设置对话框框架（tab 结构复用 T1 大设置）②params schema +
编辑 UI（先行 水平线价格/斐波那契 levels 两类打样）③图元菜单扩项（设置/克隆/复制/模板子菜单/删除）
④空白菜单扩项（复制价格格/对象树/移除全部）⑤延展 extend 引擎支持。①②③为主，④⑤可拆后续。
模板子菜单（[4b] 基准：保存为/应用默认/快速删除/点击应用）是 ③ 的核心项——KCQ 模板引擎现成，纯壳层接线。

## T1 归属统计（提示词 D 的工作清单）

- **纯壳层**：顶栏形态重做（按钮化周期/入口布局）、三分屏布局切分、状态行显隐开关、K 线配色面板、背景/网格/十字线显隐
- **需引擎扩展（core PR）**：① priceScale 对数/百分比/自动模式 ② drawing 撤销重做命令栈 ③ 时间范围条（范围→周期/根数解析） ④ 右缘空白（viewport trailing offset + timeAxis 外推 + 命中延伸）
- **不做**：TV 式保存/管理布局、快速搜索、交易/发表/事件、Logo 显隐、布局云同步
- **批次顺序更新（2026-09-18 用户拍板）**：T1 之后插入 **H 批（图元属性/参数/模板补课）**，再进 T2+
  （告警/回放/类型接线）；右键菜单扩项归 H 批。H 批提示词在 T1 提示词 D 之后写，
  前置依赖：用户补 [4b] 图元属性对话框参考图。
