# TradingView 网页版界面结构笔记（KCQ 对齐参考）

> 采集环境：cn.tradingview.com/chart/（简体中文站，免费未登录态），1680×945 视口。
> 采集方式：ZCode 内置浏览器（Chromium，隔离未登录 profile）+ DOM ARIA 快照 + 原生事件派发交互。
> 图表本体为 canvas（内部结构不可提取，也不需要）；本笔记只记录 chrome（工具栏/面板/对话框/菜单）的 DOM 结构。
> 产物仅为布局结构认知与功能文字清单；截图仅存本目录（temp/ 已 gitignore），不入库。
> 标注约定：UNAVAILABLE=免费未登录不可见；RESTRICTED=可见但功能被账号墙拦截；PARTIAL=采集手段受限未取全。

## 采集执行差异（相对提示词 C）

> 2026-09-18 第二轮深挖更新：首轮"交互受限"的根因已查明，两处结论被推翻，见 ③④。

① **Edge 通道不可用**：本环境 browser-use 仅提供 ZCode 内置浏览器（IAB，Chromium 内核）。IAB 使用隔离
干净 profile，天然满足 R1（不登录、不用用户已登录 profile）。已用 IAB 完成，红线全部满足。
② **语言切换**：`?lang=zh` 参数与 `setlang` cookie 均不改变图表应用语言；最终通过主页 hreflang 链接确认
简体中文官方站点为 `cn.tradingview.com`（zh-cn），其 /chart/ 与国际版同源同构、全 chrome 简体中文。
③ **交互失效根因（首轮误判为"合成事件不可靠"）**：真凶有二——
  a. **匿名推广浮层**（data-dialog-name="gopro"，全屏 container 拦截指针、无关闭按钮、Escape 不散、周期性弹出）。
     解法：注入 MutationObserver 自动移除该浮层后，此前全部"失效"的合成事件交互（flyout/菜单/对话框）恢复正常。
     flyout 打开、顶栏下拉等 DOM 交互对合成事件完全友好；页面内注入 `__tvClick`（完整 pointer 序列 + click）即通。
  b. **匿名绘图权限锁**：TV 免费未登录态**绘图功能整体锁定**——点击任何绘图工具弹"为您的分析解锁所有绘图/
     免费加入，访问趋势线及其他所有绘图工具"。因此画线、图元、图元右键菜单、图元属性对话框在匿名态**不存在**，
     与交互技术无关。用户登录态人工截图（[4a][4b][4c]）是这些域的唯一合法基准（R1 禁止登录）。
④ **右键菜单终验（自动化通道确认关闭）**：合成 contextmenu 派发（容器/canvas）、可信键盘 Menu 键、Shift+F10
   均无法在图表区触发 TV 右键菜单（图表手势层要求可信 pointer button-2 序列）。右键域基准只能人工截图。
⑤ 交互手段有效域总结（匿名态）：
   - 可信 cua/合成 __tvClick 均可：顶栏按钮+下拉（部分）、左工具栏 flyout、图表属性对话框、tab 切换、右栏面板、底部条按钮
   - 合成事件无效（isTrusted 门控或功能锁定）：绘图工具激活（锁定）、图表区右键（门控）、指标模板/管理布局菜单（无响应）
⑥ 页面导航计数：两轮合计约 13 次加载，满足 R2 < 30。

---

## [1] 顶栏（header toolbar）

截图：`01-chart-full-cn.png`（整页中文初始态）、`01-topbar.png`（顶栏特写）、`01b-indicators-dialog.png`（指标弹层）

入口：图表页顶部，通栏一行（高约 38px），左侧品种区 → 中部功能按钮区 → 右部全局操作区。

控件清单（自左向右，按 TV 稳定元素 id 标注）：

| 控件 | 文字/提示 | 元素 id | 说明 |
|---|---|---|---|
| 品种搜索 | AAPL（当前代码+徽章） | header-toolbar-symbol-search | 打开商品代码搜索弹层 [10] |
| 切换数据类型 | 切换数据类型 | header-toolbar-switch-data-type | 行情源切换（BATS/Cboe One 等） |
| 比较商品 | （图标） | header-toolbar-compare | 打开比较弹层 [10b] |
| 周期快捷 | "1 日"（当前值"天"） | 无（menuBtn） | 打开周期菜单 [9b] |
| K线类型 | K线图 | 无（menuBtn） | 打开类型菜单 [9c] |
| 指标 | 指标 | 无（withText 按钮） | 打开指标/度量/策略弹层 [1b] |
| 指标模板 | （图标） | 无 | 指标模板管理 |
| 创建警报 | 警报 | header-toolbar-alerts | 右侧栏警报面板 + 升级墙 [7] |
| K线回放 | 回放 | header-toolbar-replay | 回放模式开关 [8] |
| 撤销 / 重做 | （图标，禁用态） | 无（isDisabled） | 无操作时 disabled；T1 目标 |
| 布局设置 | （图标） | header-toolbar-layouts | 布局网格菜单 [11] |
| 保存布局 | 保存 | 无 | 保存当前布局全部图表 |
| 管理布局 | （图标） | 无 | 布局管理器 |
| 快速搜索 | （图标） | header-toolbar-quick-search | 全局搜索 |
| 设置 | （齿轮） | header-toolbar-properties | 图表属性对话框 [3] |
| 全屏模式 | （图标） | header-toolbar-fullscreen | |
| 生成快照 | （图标） | 无 | 图表快照导出 |
| 交易 | 交易 | 无（activeArea） | 交易面板入口 |
| 发表 | 发表 | 无（menuWrap） | 社区观点发布 |

指标弹层 [1b] 结构（dialog "指标、度量与策略"）：搜索框 + 左侧分类树（全部/收藏/指标/策略/我的）+ 右侧列表
（每行：名称 + 指标热度 + 收藏星标），底部"确认"按钮。

## [2] 左侧绘图工具栏（drawing toolbar）

截图：`02-drawing-toolbar-collapsed.png`（收起态特写）、`02a-flyout-cursors.png`、`02-flyout-trend.png`、
`02-flyout-gann-fib.png`、`02-flyout-patterns.png`、`02-flyout-forecast-measure.png`、`02-flyout-geometric.png`、
`02-flyout-annotation.png`、`02-flyout-icons.png`（后三张为第二轮深挖补采，浮层拦截解除后成功）。
**8 组 flyout 全部采集完成，工具名清单完整无缺。**

入口：图表区左缘竖条（宽 52px），每工具组 = 主按钮(38×38) + 右下角小箭头(11×34，点开组 flyout)。
布局：上 13 组工具（自上而下）→ 分隔 → 测量/放大 → 分隔 → 6 个模式开关。

组清单与展开态工具名（treegrid 结构，每行含"点击这里了解更多"+快捷键+添加到收藏）：

1. **光标**（十字线*）：十字线、圆点、箭头、荧光笔、橡皮擦 ＋ 开关"长按时显示数值工具提示"
2. **趋势工具**（趋势线*）：
   - 线：趋势线(Alt+T)、射线、信息线、延长线、趋势角度、水平线(Alt+H)、水平射线(Alt+J)、垂直线(Alt+V)、横线(Alt+C)
   - 通道：平行通道、回归趋势、平顶/平底、不相交通道
   - 分叉线：分叉线、希夫干草叉、改良希夫干草叉、内部分叉线
3. **江恩和斐波那契工具**（斐波那契回撤*）：
   - 斐波那契：回撤、趋势扩展、通道、时区、速度阻力扇、趋势时间、圈、螺旋、速度阻力弧、楔形、倾斜扇形
   - 江恩：江恩箱、固定江恩正方、江恩正方、江恩扇
4. **形态**（XABCD形态*）：
   - 图表形态：XABCD形态、赛福形态、头肩形态、ABCD形态、三角形态、三驱形态
   - 艾略特波浪：脉冲波(1·2·3·4·5)、修正浪(A·B·C)、三角波(A·B·C·D·E)、双重组合浪(W·X·Y)、三重组合浪(W·X·Y·X·Z)
   - 循环：循环线、时间循环、正弦线
5. **预测和测量工具**（多头仓位*）：
   - 预测：多头仓位、空头仓位、仓位预测
   - K线形态：模拟K线
   - 扇形：基于成交量
   - 基于成交量：锚定VWAP、固定范围成交量分布图、锚定成交量分布图
   - 测量：价格范围、日期范围、日期和价格范围
6. **几何形状**（笔刷*）：已补采（第二轮深挖，浮层拦截解除后成功）——
   - 笔刷：笔刷、荧光笔
   - 箭头：箭头标记、箭头、向上箭头、向下箭头
   - 形状：矩形、旋转矩形、路径、圆、椭圆、折线、三角形、弧形、曲线、双曲线
7. **注释工具**（文字*）：已补采——
   - 文本和笔记：文字、笔记、价格笔记、置顶
   - 内容：表格、标注、评论、价格标签、指示牌、旗标、图片、帖子、观点
8. **图标**：已补采——结构不同：**三 tab 网格**（表情符号 / 贴纸 / 图标），图标本体无 ARIA 名（纯图形网格）

独立工具与模式开关：

- 测量、放大（独立按钮）
- 磁铁（开关，提示"磁铁模式会将放置在价格K线附近的绘图吸附到最近的OHLC值"，带"磁铁"档位下拉）——与 KCQ 引擎磁吸语义对应
- 继续绘图（开关，= KCQ stay 保持绘图模式）
- 锁定绘图和指标（开关 + 锁定选项下拉）
- 隐藏所有图形（开关 + 隐藏选项下拉）
- 移除对象（按钮 + 移除选项下拉）

## [3] 图表属性对话框（设置）

截图：`03-chart-settings.png`（商品代码 tab）、`03b-settings-statusline.png`、`03c-settings-scales.png`、
`03c2-scale-mode-options.png`（坐标可见性下拉）、`03d-settings-appearance.png`、`03e-settings-trading.png`、
`03f-settings-alerts.png`、`03g-settings-events.png`

入口：顶栏齿轮（header-toolbar-properties）；结构：居中模态 + 左侧竖排 tab 列（7 tab，**每 tab 带图标**，
选中态浅色高亮背景；标题"设置"，右上角仅 ✕）+ 底部"模板 ∨ / 取消 / 确认"。

- **商品代码**：K线图（K线颜色基于前一收盘价 / 主体✓ / 边框✓ / 影线✓，各带双色选钮）；
  数据修改（调整股息数据✓ + 了解更多链接）；精确度（系统预设）；时区（(UTC+8) 上海）
- **状态行**：商品（Logo✓ / 标题✓ / 名称下拉 / 图表值✓ / K线变化值✓ / 成交量 / 最后一天变化值 / 买-卖按钮✓，
  附说明"图表上直接显示买入和卖出按钮"）；指标（标题✓ / 输入✓ / 数值✓ / 背景✓）
- **坐标和线条**：价格坐标（货币和单位 / 坐标模式（A和L）/ 锁定价格对K线比例 + 比例输入 0.884 / 坐标放置=自动）；
  价格标签和价格线（无重叠标签✓ / 加号按钮✓ / 当前K线结束倒计时✓）；标签值粒度（商品代码=值,线形图 + "根据坐标值"下拉；
  指标和财务数据=值；盘前/盘后/夜间市场=值,线形图）；高点和低点=隐藏；Bid和Ask=隐藏；
  时间坐标（标签上的星期几✓ / 日期格式="周一 1997-09-29" / 时间小时格式=24小时 / 改变周期时保存图表左边缘位置）
  - 坐标可见性下拉选项（03c2）：鼠标移动时可见 / 总是显示 / 总是隐藏
  - 图表右缘价格轴上另有快捷按钮：切换自动坐标、切换对数坐标（采集于坐标枚举，T1 对数/百分比/自动缩放的直接参照）
- **版面**：图表基本样式（背景=Solid+选色 / 垂直网格线✓ / 水平网格线✓ / 十字线 / 水印 / 回放模式 / 坐标-文本-线条-按钮）；
  文本字号（12）；导航（鼠标移动时可见）；窗格（鼠标移动时可见）；利润率（顶部 10% / 底部 8% / 右 10% / 根K线）
- **交易**：普通（买-卖按钮✓ / 一键交易 / 执行声音+滑杆+闹铃 / 仅显示拒绝通知）；
  外观（仓位和订单✓ / 反向仓位按钮✓ / 市价单的项目订单✓ / 盈利和亏损值✓ / 持仓=资金 / 包围单=资金 / 执行标记✓ /
  执行标签 / 延伸价格线至整个图表宽度✓）；订单和仓位对齐=右；图表快照中的订单、执行和仓位
- **警报**：图表线可见性（警报线✓ / 仅活动警报✓）；通知（警报音量✓+滑杆 / 自动隐藏弹窗✓）
- **事件**：观点（+所有观点下拉，未勾选）/ 股利✓ / 拆分✓ / 收益✓ / 收益间隔 / 最新消息✓ / 新闻通知

### [3h] 设置对话框的模板下拉展开态（用户人工补图 2026-09-18，登录态 BTCUSD）

来源：`03h-settings-template-dropdown.png`（左下角"模板 ^"展开）。

- **下拉菜单结构**：应用默认 ／ 保存为... ／ ── ／ 模板列表（点击即应用整图模板）
- **模板命名规律**：默认按保存日期自动命名（2023-05-27、2024年1月18日、2025-05-22…），可自定义名并存
  （Black、BRG、G、Grey、JL、2025年8月12日 footprint…）——列表可滚动，量大的用户靠日期回溯
- **域区分**：此下拉管理的是**图表模板**（整图快照：K线样式+坐标+指标集+事件标记等设置页全部内容），
  与绘图模板（[4b] 图元右键菜单，per-kind 存样式）、指标模板是三个不同域
### [3i] 模板列表项的快速删除（用户人工补图 2026-09-18）——模板管理统一范式

来源：`03i-template-remove-hover.png`（hover 模板列表项 "JL" 时，右侧浮现垃圾桶图标 + 红色"移除" tooltip）。

- **修正 [3h] 推测**：图表模板下拉列表项**支持 hover 垃圾桶一键移除**，无需进管理界面——
  与 [4b] 绘图模板子菜单的垃圾桶行为完全一致
- **统一范式结论（用户拍板可照搬）**：TV 的模板管理在三种域（图表模板/绘图模板/指标模板）是**同一套交互**：
  `应用默认 + 保存为... + 列表（点击应用 / hover 垃圾桶删除）`，仅作用域不同——
  H 批实施应做成一套可复用的模板管理组件（入口三处：右键菜单子菜单/对话框底部下拉/面板），仅数据域不同
- 同图另证：设置对话框"商品代码" tab 的 K线图样式区（主体/边框/影线各 ☑+双色选钮）与我匿名态采集（[3]）一致

## [4] 图表右键菜单

### [4a] 图表空白处右键菜单（用户人工补图 2026-09-18，登录态 XAUUSD）

来源：`04a-blank-context-menu.png`（用户登录态截图；另附 TV 新版底部浮动绘图工具条形态可作 [2] 补充观察）。
菜单结构（14 项，5 组）：

1. **重置图表视图**（Alt + R）
2. ---
3. **复制价格格** 4,611.29 ／ **粘贴**（Ctrl + V）
4. ---
5. **以 4,611.29 在 XAUUSD 上添加警报...**（Alt + A）／ **卖出 1 XAUUSD @ 4,611.29 限价**（Alt + Shift + S）／
   **买入 1 XAUUSD @ 4,611.29 止损** ／ **以 4,611.29 添加订单到 XAUUSD...**（Shift + T）
   ——⚠️ 交易三项为登录态专属（匿名态无），且 KCQ 无交易场景，实施时不采
6. ---
7. **按时间锁定垂直光标线**
8. ---
9. **表格视图** ／ **对象树** ／ **图表模板**（▸ 子菜单）
10. **移除 71 个绘图**（数量动态）／ **移除 1 个指标**（数量动态）
11. ---
12. **设置...**（齿轮 → 打开图表属性对话框 [3]）

要点：菜单项按右键坐标上下文生成——警报/交易三项携带右键处价格值；"移除 N 个绘图/指标"带实时计数；
"表格视图/对象树"是面板开关入口；"图表模板"是整图模板管理（与绘图模板不同域）。

### [4b] 图元右键菜单 + 模板子菜单（用户人工补图 2026-09-18，登录态，命中的是水平射线）

来源：`04b-drawing-context-menu.png`。主菜单结构（11 项，4 组）：

1. **为水平射线 添加警报...**（Alt + A）——菜单项按图元 kind 动态生成（"为<图元名> 添加警报"）
2. ---
3. **模板**（▸）／ **视觉顺序**（▸）／ **时间周期可见性**（▸）／ **对象...**（▸，语义待确认——疑为对象树定位/移动到）
4. ---
5. **克隆**（Ctrl + Drag）／ **复制**（Ctrl + C）
6. ---
7. **锁定** ／ **隐藏** ／ **移除**（Del）
8. ---
9. **设置...**（齿轮 → 图元属性对话框）

**模板子菜单**（展开态，本图核心增量）：

- **保存为...** —— 把当前图元样式存为新模板
- **应用默认** —— 恢复该 kind 的默认样式
- **当前模板名 + 垃圾桶图标** —— 快速删除（图中"确认 🗑"行；垃圾桶 = 一键删除该模板）
- 分隔后为**已存模板列表**（点击即应用到当前图元）：上 / 下 / 注意 / LQ / LQ H1 / LQ used / OB / POC / SR / TP
  （用户自建的 10 个水平线样式模板——交易关键位场景：LQ 流动性、OB 订单块、POC、SR、TP 等，每模板存不同颜色/线型）

要点：模板调用零跳转（右键→点模板名即应用）；管理与调用同层（保存为/应用默认/删除都在子菜单内）。
另：截图右下角同帧拍到 **TV 选中图元浮动条**（样式/文本/线宽 2px/设置/删除——对应 KCQ Flybar 形态）。

### [4c] 图元属性对话框（用户人工补图 2026-09-18：斐波那契回撤"样式"tab 全录）

来源：`04c-fib-settings-dialog.png`。对话框骨架与设计要点（H 批 params 编辑的直接基准）：

- **标题栏**：图元 kind 名（"斐波那契回撤"）+ 图钉（固定对话框）+ 关闭 ✕
- **tab 结构**：样式 ／ 坐标 ／ 可见范围（三个；本图为"样式"tab）
- **底部**：模板 ∨ 下拉（对话框内也集成模板管理——TV 模板入口共 4 处：右键菜单子菜单/本对话框/工具栏/Flybar）
  + 取消 ／ 确认

"样式" tab 内容分组：

1. **线段组**：趋势线 ☑+色钮（首段趋势线）；水平线（两组线型选钮）
2. **延伸**：下拉"不要扩大"（延伸方向）
3. **levels 档位表**（核心，两列布局每行三控件）：
   ☑ 显隐 + **数值输入框（可编辑，0 档呈 spinner 编辑态）** + **独立颜色选钮**
   预置档位：0/0.236/0.382/0.5/0.618/0.75/0.8/1/1.618/2/2.272/2.414/2.618/3/3.272/3.414/3.618/4/4.272/4.414/4.618/5.618/±0.618/±1.414（部分勾选部分隐藏）
   ——即档位数值本身是参数（可增改删），非固定枚举
4. **使用一种颜色** 开关（统一所有档位颜色，覆盖逐档配色）
5. 背景 ☐ + 透明度滑杆；反手 ☐（fib 方向反转）
6. **标签组**：价格 ☑；水平位 ☑ + 格式下拉"百分比"（水平位数值显示格式：百分比/价格/绝对）；标签位置（右/中间）；
   文本 ☑ + 对齐（中心/中间）；字体大小 12
7. **基于对数坐标的Fib水平 ☑** —— fib 档位与坐标缩放联动（与 T1 对数坐标扩展直接相关：勾选后档位在对数轴等距）

**仍待补**（可选，最后两张）："坐标" tab（图元锚点的时间/价格编辑——水平线价格值、fib 两端锚点，H 批"改水平线价格"的打样基准）
与"可见范围" tab（按周期显隐勾选列表）；或任一水平线的属性对话框（坐标 tab 更直观）。

## [5] 对象树 / 对象管理器

截图：`05-object-tree.png`（空态结构）、`05-object-tree-with-drawing.png`（尝试画线后的状态，实际未产生图形——工具选中需可信事件，见执行差异）

入口：右侧图标栏"对象树和数据窗口"按钮（与数据窗口同面板双 tab）。

- 面板结构：tablist（对象树 | 数据窗口）→ 工具条 → 树列表
- 工具条按钮（无对象时全 disabled）：建立一组绘图、克隆，复制、移动到、管理布局绘图
- 树列表：空态无条目（未登录无历史图形；画线失败未产生条目）
- 每对象的操作项：未能观察（无对象）；TV 惯例为可见性/锁定/删除/设置，**未验证不写死**

## [6] 数据窗口（Data Window）

截图：`06-data-window.png`（十字线联动态）

入口：右侧栏"对象树和数据窗口" → 切"数据窗口" tab。

- 顶部：日期行（十字线所在 K 线，如"周四 2026-03-05"）
- 品种行：`AAPL · 1天 · NASDAQ` + 行内"隐藏数据"按钮 + 键值对：开盘价/最高价/最低价/收盘价/涨跌/成交量/最后一天变化
- 指标行（每个指标一组）：`成交量(Volume)` + "隐藏数据" + Volume 值
- 交互要点：十字线移动时全量联动刷新；无十字线时显示最新 K 线

## [7] 告警面板 / 创建告警弹层

截图：`07-alerts.png`。RESTRICTED（账号墙）。

- 入口：顶栏"创建警报"或右侧栏"警报"
- 可见部分：条件选择按钮"收盘价"、升级提示区（标题"再也不会错过任何交易"，文案
  "即使远离您的办公桌，也可以通过我们的即时和备份警报掌握市场活动的最新情况 — 仅适用于 Essential 或升级方案"）、
  "免费加入"按钮、"了解有关此功能的更多信息"链接
- 结论：匿名态告警创建不可用，仅可见入口与条件选择器形态。引擎侧 KCQ 已有 features/alerts（未接壳），UI 参照此形态。

## [8] 回放（Bar Replay）

截图：`08-replay.png`。RESTRICTED（账号墙）。

- 入口：顶栏"K线回放"按钮
- 点击后回放模式**可激活**（图例 OHLC 冻结为 ∅，数据停流），但控制条被账号墙替换：
  "解锁K线回放 / 获得我们的免费帐户并观察定价数据的历史发展情况。以您想要的速度回放和回顾市场。/ 免费加入"
- 控制条形态（播放/暂停/速度/进度）匿名不可见 → UNAVAILABLE 部分。KCQ 引擎已有 features/replay（未接壳）。

## [9] 底部周期条与时间轴工具

截图：`09-bottom-bar.png`（特写）、`09b-period-menu.png`（顶栏周期菜单）、`09c-chart-type-menu.png`（类型菜单）

- 底部条（图表下方通栏）：时间范围快捷 1天/5天/1个月/3个月/6个月/YTD/1年/5年/全部（每项 tooltip 形如
  "1分钟时间周期内的1天"= 范围与周期联动）→ "前往到" → 右侧"时区"（显示当前 03:52:11 UTC+8）→ "调整股息数据 ADJ"
- **前往到弹层**（已采）：dialog，双 tab（日期 [selected] / 自定义范围）+ 日期输入框（YYYY-MM-DD）+ 时间下拉
  （匿名禁用）+ 月历网格（上月/下月/切月按钮 + 周一~周日表头 + 日期 cell）——跳转日期功能 UI 全貌
### [9d] "滚动到最近的K线"按钮（用户人工补图 2026-09-18，登录态 BTCUSD）

来源：`09d-scroll-to-latest.png`、特写 `09d2-scroll-plus-closeup.png`。

- **出现条件**：鼠标移动到图表右缘、接近价格轴时浮现（视口处于历史位置、最新 K 线不在可视区时是主要场景）
- **形态**（特写图）：两个悬浮钮并排——**⊕ 加号按钮**（深色圆钮：快捷添加警报/订单，即图表属性"加号按钮"
  开关控制的那个）+ **» 滚动按钮**（白色圆钮，向右双箭头）；右侧紧贴价格轴当前价标签
- **Tooltip**：» 按钮 = 滚动到最近的K线（**Alt + Shift + →**）；旁边小 ∨ 仅为悬浮说明的指示箭头，**不是下拉**
- **行为**：点击 » 后视口水平跳转、右缘对齐最新 K 线；配合实时数据保持跟随
- **与右缘空白的关系**：与 [E] 域右缘空白扩展是同一对功能——右缘空白允许拖到历史/未来区浏览，
  本按钮提供一键回到实时位置的回路

### [9e] 价格轴 hover 快捷钮 A / L（用户人工补图 2026-09-18）

来源：`09e-price-axis-AL-buttons.png`（右下角红框处）。

- **出现条件**：鼠标 hover 到价格轴区域，轴下端浮现两个小字母钮
- **A = 自动比例**（Auto scale：按可见区数据自适应缩放）；**L = 对数坐标**（Logarithmic：价格轴对数刻度）
- **交互意义**：一键切换、hover 即达——用户明确反馈"KCQ 把对数坐标埋得太深，很不方便"，T1 对数/自动模式
  必须同时提供此轴上快捷钮形态（与图表属性对话框"坐标模式"下拉是同一状态的两种入口）
- 现状：KCQ 引擎 priceScale 无 log/percent 模式（T1 需引擎扩展），壳层无任何入口

- 顶栏周期菜单 [9b]（分组 + 每项可收藏）：
  Ticks：1/10/100/1000 ticks；秒：1/5/10/15/30/45；分：1/2/3/5/10/15/30/45；小时：1/2/3/4；
  天：1日/1周/1月；范围：3月/6月/12月、1/10/100/1000 范围
- K线类型菜单 [9c]（21 种，每项带"点击这里了解更多"+收藏）：美国线、K线图、空心K线图、成交量蜡烛、线形图、
  带标记线、阶梯线、面积图、HLC区域、基准线、柱状图、高-低、成交量轨迹、时间价格机会、交易时段成交量分布图、
  平均K线图、砖形图(Renko)、新价线、卡吉图、点数图(PnF)、范围图(RangeBars)
  ——后三域 KCQ 引擎 chartTypes 已有（Renko/RangeBars/PnF 未接壳）

## [10] 品种搜索 / 比较弹层

截图：`10-symbol-search.png`、`10b-compare-dialog.png`

- 商品代码搜索（顶栏 AAPL）：模态，searchbox（占位"商品代码、ISIN或CUSIP"）+ 清除按钮 + "显示价差运营商"开关 +
  资产类别 tab：全部/股票/基金/期货/外汇/加密货币/指数/债券/经济/期权 + 更多；结果行 = 代码徽章 + 全名 + 类型标签
  （stock/dr/fund etf/structured/index）+ 交易所徽章（NASDAQ—纳斯达克证券市场 等）+ "延长"按钮；底部"使用ISIN和CUSIP代码搜索"
- 比较商品（顶栏比较按钮）：同款搜索框 + "最近的商品"列表（SPX/NDQ/DJI/NI225/DEU40/UKX…），选中即叠加对比

## [11] 布局切换入口

截图：`11-layouts.png`

入口：顶栏"布局设置"。菜单（menu + menuitemradio 单选组，按格数分组）：

- 1 格：s（当前✓）
- 2 格：2h / 2v
- 3 格：3h / 3v / 3s / 3r / 2-1 / 1-2
- 4 格：4 / 4v / 4h / 4s / 4s-l / 1-3 / 3-1 / 2-2-l / 2-2-r / 2-2
- 5 格：1-4 / 5h / 5v / 5s / 5s-l / 2-3 / 3-2 / 4-1 / 2-3-l / 2-3-r
- 6 格：6 / 6h / 6v / 6c / 2-4 / 4-2
- 7 格：4-3 / 7h / 7s；8 格：8 / 8c / 8h / 8v；9 格：9s / 5-4 / 9h…

匿名态菜单全量可见；官方免费版实际可用 1-2 格，3+ 格选择会触发升级（未实际点击验证，保守记录）。
KCQ T1 拍板"多布局上限三分屏"，参照 1 格 s、2 格 2h/2v、3 格 3h/3v/2-1/1-2 子集即可。

## [12] 加分项：Watchlist 详情 / 图标库

- Watchlist + 详情（右侧栏"自选表、详情和新闻"，DOM 完整采集，无单独截图——见 `01-chart-full-cn.png` 右栏）：
  表头（自选表 / 添加商品代码 / 高级视图 / 设置）+ 可排序列（商品/最新价/涨跌/涨跌%）；
  默认表按 股票/期货/外汇/加密货币 分组（000001 上证、HSI、SPX、GOLD、EURUSD、BTCUSD…每行代码徽章+延迟标记 D+状态）；
  详情页各节：商品头（全名+交易所+板块行业链接+现价+涨跌）、新闻、关键统计（下一份财报/成交量/平均成交量(30)/市值）、
  收益（季度柱图 公布值/预估）、股利（派息率TTM/股息收益率/最后除息日）、损益表（年度/季度切换）、表现（1W~1Y）、
  季节性、技术指标（强烈卖~强烈买标尺）、分析师评级、1年价格目标、更多（债券/期权 IV）
- 图标库（绘图工具"图标"组）：UNAVAILABLE-技术（flyout 交互受限，同 [2] 后三组）

## 附 A：匿名态锁定域清单（自动化采集边界，提示词 D/H 引用）

以下域在免费未登录态被 TV 锁定或不可达（均已实测验证），自动化不可采；基准以用户登录态截图为准：

| 域 | 锁定形态 | 已有人工基准 |
|---|---|---|
| 绘图工具激活（任意绘图工具） | 点击弹"为您的分析解锁所有绘图"升级墙 | [4b][4c]（图元菜单/属性对话框） |
| 图元（画线后才有的一切：对象树条目/图元右键/图元属性/图元浮动条） | 无图元即无 UI | [4b][4c] |
| 图表空白处右键菜单 | contextmenu 需可信 button-2 指针序列 | [4a] |
| 告警创建 / 回放控制条 | 账号墙（"再也不会错过任何交易"/"解锁K线回放"） | [7][8]（入口+文案） |
| 3+ 格布局实际选择 | 菜单可见，选择触发升级（未点击验证） | [11]（菜单结构全录） |
| 指标模板 / 管理布局菜单 | 点击无响应（匿名态空实现） | 无（低价值，KCQ 不做清单） |

匿名态自动化**可采且已采**：顶栏全部按钮与主下拉（品种/比较/周期/类型/指标/布局网格/设置对话框）、
左工具栏 8 组 flyout 全部工具名、对象树/数据窗口面板、底部条（周期条/前往到/时区按钮）、
右侧栏（自选表+详情全节）、比较弹层、指标弹层。

## 附 B：页面整体布局层次（自外向内）

```
header toolbar（顶栏，[1]）
├─ drawing toolbar（左竖条，[2]）
├─ chart area
│   ├─ 图例栏（变更品种/变更周期/交易所/OHLC 行情行/买-卖报价 + 指标行 隐藏/设置/移除/更多）
│   ├─ canvas 图表本体
│   ├─ 右缘价格轴（切换自动坐标/切换对数坐标 快捷钮）
│   └─ 底部周期条（[9]）
├─ right icon rail（右侧图标栏）：自选表详情新闻/警报/对象树和数据窗口/筛选器/Pine/日历/社区/通知/产品/帮助中心
├─ right panel（Watchlist+详情 或 对象树/数据窗口 或 警报）
└─ 交易面板（Trading panel，底缘）
```
