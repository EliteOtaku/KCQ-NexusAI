/**
 * Agent Markdown 渲染模块的公共入口：保持既有 `render-agent-markdown` 导入路径可用，
 * 实现与类型已迁至 `render-agent-markdown/`。
 */
export { renderAgentMarkdown } from './render-agent-markdown/impl/render-agent-markdown.js'
